(function(window){
"use strict";

window.cnri = window.cnri || {};
cnri.util = cnri.util || {};

function ChainBuilder(handleMap, client) {
    var self = this;
    
    function constructor() {
    }
    
    function buildChain(childSignature) {
        
    }
    self.buildChain = buildChain;
    
    constructor();
}

cnri.util.ChainBuilder = ChainBuilder;
/*end*/})(this);
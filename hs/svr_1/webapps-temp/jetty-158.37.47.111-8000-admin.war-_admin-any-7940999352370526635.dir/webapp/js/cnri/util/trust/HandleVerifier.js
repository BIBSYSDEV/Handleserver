(function(window){
"use strict";

window.cnri = window.cnri || {};
cnri.util = cnri.util || {};

function HandleVerifier() {
    var self = this;
    
    function constructor() {
    }
    
    function verifyValues(handle, values, signature, publicKey) {
        
    }
    self.verifyValues = verifyValues;
    
    function getHandleClaimsSet(signature) {
        
    }
    self.getHandleClaimsSet = getHandleClaimsSet;
    
    constructor();
}

cnri.util.HandleVerifier = HandleVerifier;
/*end*/})(this);
(function(window){
"use strict";

window.cnri = window.cnri || {};
cnri.util = cnri.util || {};

function ChainVerifier(rootKeys) {
    var self = this;
    
    function constructor() {
    }
    
    function verifyValues(handle, values, issuedSignatures) {
        
    }
    self.verifyValues = verifyValues;
    
    function verifyChain(issuedSignatures) {
        
    }
    self.verifyChain = verifyChain;
    
    constructor();
}

cnri.util.ChainVerifier = ChainVerifier;
/*end*/})(this);